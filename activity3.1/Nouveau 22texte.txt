ALTER TABLE film
ADD nb_court-metrage VARCHAR(20) NULL, column_c INT NULL ;

SELECT COUNT(duree_min < 42) FROM film AS nb_court-metrage;

SELECT titre, resume, annee_prod 
FROM film 
INNER JOIN genre ON Id;

SELECT * FROM film JOIN genre ON film.id_genre = genre.id_genre where genre.nom='erotic' ORDER BY annee_prod DESC;
SELECT  titre AS Titre, resum AS Resume , annee_prod FROM film JOIN genre ON film.id_genre = genre.id_genre where genre.nom='erotic' ORDER BY annee_prod DESC;

SELECT nom, prenom FROM `fiche_personne`  WHERE nom LIKE "%-%" or prenom LIKE "%-%" ORDER BY nom, prenom ASC;

SELECT salle, SUM(nbr_siege) AS sieges
FROM salle
GROUP BY etage_salle;